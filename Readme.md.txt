1)I have used angular seed for bootstrap
so can install using npm in git bash and see the output
Commands:

-->cd etl_app
-->npm install
-->npm start
if not working check connectivity with github and npm server

 or can open browser directly(if cross domain not occured), or put into any webserver and hit


2)data and mapping files also present in etl_csv folder
3)added output screenhsot also