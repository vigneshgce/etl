'use strict';

describe('myApp.etl module', function() {

  beforeEach(module('myApp.etl'));

  describe('etl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var etlController = $controller('etlController');
      expect(etlController).toBeDefined();
    }));

  });
});